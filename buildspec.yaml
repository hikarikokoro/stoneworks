version: 0.2

phases:
  pre_build:
    commands:
      - echo Installing source NPM dependencies...
      - npm install
      - npm install --location=global @angular/cli
  build:
    commands:
      - echo Build started
      - ng build --prod
artifacts:
  files:
    - "**/*"